<?php
    // À compléter avec vos identifiants SMTP (mot de passe d’application nécessaires avec Gmail)
    $EMAIL = "";
    $PASSWORD = "";

    return [
        "smtp_username" => $EMAIL,
        "smtp_password" => $PASSWORD,
        "from_email" => $EMAIL,
        "to_email" => $EMAIL,
    ];
